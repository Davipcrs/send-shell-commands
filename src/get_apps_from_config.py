# Create a .conf file.
# That conf file is to be saved on the Home.
# the .conf file will get a app name (in linux) or a app exe path (in windows)
# the .conf file will get a the argv (in google-chrome a 'google.com' url) (linux)
# the .conf file will get a URL for a image of that program logo.
